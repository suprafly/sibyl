# Amendment: adaptive recovery escalation

Page recovery processes each accepted coarse text region independently. It
starts with a small baseline budget, performs targeted baseline rereads only
for unresolved regions, and performs native-reference escalation only when
the existing conservative selector still cannot accept a reading. Recovery
records stage decisions and compute counts while preserving all raw evidence.
