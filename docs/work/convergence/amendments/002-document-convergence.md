# Amendment: document-level convergence

Add a second deterministic pass over regional candidates and alternatives. It
must preserve the regional layer, use spatial reading order and observed
page-level evidence where available, expose explainable scores and provenance,
support evidence-backed phrase extension, and enforce that every emitted token
has a recognition or explicit human-review path. Do not introduce semantic
oracles, external vocabularies, model calls, or specimen-specific rules.
