# Amendment: transform artifact regression

Markdown projection must preserve the complete structured transform artifact
as `<stem>.sibyl/transform.json`, alongside `transform.md` and original-source
figure assets. JSON remains the canonical CLI projection of the existing
structured transform. Mocked tests must verify persistence, asset extraction,
Markdown references, and separate Qwen/TrOCR disagreement evidence. No model
inference or OCR tuning is authorized for this amendment.
