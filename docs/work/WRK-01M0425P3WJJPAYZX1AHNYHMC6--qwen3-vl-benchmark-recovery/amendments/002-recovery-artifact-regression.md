# Amendment: recovery artifact regression

Markdown projection must preserve the complete structured recovery artifact
as `<stem>.sibyl/recovery.json`, alongside `recovery.md` and original-source
figure assets. JSON remains the canonical CLI projection of the existing
structured recovery. Mocked tests must verify persistence, asset extraction,
Markdown references, and separate Qwen/TrOCR disagreement evidence. No model
inference or OCR tuning is authorized for this amendment.
