[project]
name = "sibyl"
version = "0.1.0"
description = "Faithful transform of handwritten material into structured digital artifacts."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "accelerate==1.14.0",
  "pillow==12.3.0",
  "sentencepiece==0.2.2",
  "torch==2.13.0",
  "torchvision==0.28.0",
  "transformers==4.57.6",
]

[project.scripts]
sibyl = "sibyl.cli:main"

[dependency-groups]
dev = [
  "mypy>=1.11",
  "pytest>=8.0",
  "ruff>=0.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
line-length = 100
target-version = "py313"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.13"
strict = true
files = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
